# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.


##### Данные

В приложении используются две основные сущности: товар и покупатель.

Интерфейс товара
`interface IProduct {id: string; description: string; image: string; title: string; category: string; price: number | null;}`

Поля:

`id: string` — уникальный идентификатор товара.
`description: string` — описание товара.
`image: string` — ссылка на изображение товара.
`title: string` — название товара.
`category: string` — категория товара.
`price: number | null` — цена товара. Если значение null, товар недоступен для покупки.

Интерфейс покупателя
`type TPayment = 'card' | 'cash';`

`interface IBuyer {payment: TPayment; email: string; phone: string; address: string;}`

Поля

`payment: 'card' | 'cash'` — способ оплаты.
`email: string` — электронная почта покупателя.
`phone: string` — номер телефона.
`address: string` — адрес доставки.

##### Модели данных

В приложении реализованы три модели данных:

Каталог товаров (ProductCatalog)
Корзина (Basket)
Покупатель (Buyer)

Каждая модель отвечает только за хранение и управление своими данными.

Класс `ProductCatalog`
Класс не принимает параметров.

Класс отвечает за:
хранение списка всех товаров;
хранение выбранного товара для детального просмотра;
предоставление методов для получения и сохранения данных каталога.

Поля
`private products: IProduct[];`
`private selectedProduct: IProduct | null;`

`products` — массив всех товаров каталога.
`selectedProduct` — товар, выбранный для детального просмотра.

Методы
`setProducts(products: IProduct[]): void` - Сохраняет массив товаров.

`getProducts(): IProduct[]` - Возвращает массив всех товаров.

`getProductById(id: string): IProduct | undefined` - Возвращает товар по его id.

`setSelectedProduct(product: IProduct): void` - Сохраняет товар для детального просмотра.

`getSelectedProduct(): IProduct | null` - Возвращает выбранный товар.


Класс `Basket`
Класс не принимает параметров.

Класс отвечает за:
хранение товаров, добавленных в корзину;
управление содержимым корзины;
расчёт общей стоимости и количества товаров.

Поля
`private items: IProduct[];`

`items` — массив товаров, добавленных в корзину.

Методы
`getItems(): IProduct[]` - Возвращает массив товаров в корзине.

`addItem(product: IProduct): void` - Добавляет товар в корзину.

`removeItem(product: IProduct): void` - Удаляет товар из корзины.

`clear(): void` - Очищает корзину.

`getTotalPrice(): number` - Возвращает сумму всех товаров в корзине (товары с price = null не учитываются).

`getItemsCount(): number` - Возвращает количество товаров в корзине.

`hasItem(id: string): boolean` - Проверяет наличие товара в корзине по его id.


Класс `Buyer`
Класс не принимает параметров.

Класс отвечает за:
хранение данных покупателя;
обновление отдельных полей;
валидацию данных;
очистку данных после успешного заказа.

Поля
`private payment: TPayment | null;`
`private email: string;`
`private phone: string;`
`private address: string;`

`payment` — выбранный способ оплаты.
`email` — электронная почта.
`phone` — телефон.
`address` — адрес доставки.


Методы
`setData(data: Partial<IBuyer>): void` - Позволяет обновлять одно или несколько полей без перезаписи остальных.

`getData(): Partial<IBuyer>` - Возвращает текущие сохранённые данные покупателя.

`clear(): void` - Очищает все поля покупателя.

`validate(): Partial<Record<keyof IBuyer, string>>`

Выполняет проверку данных.
Возвращает объект с ошибками.

Пример результата:

`{`
  `payment: 'Не выбран способ оплаты',`
  `email: 'Укажите email'`
`}`

Если ошибок нет — возвращается пустой объект {}.

Правила валидации:

поле считается валидным, если оно не пустое;
способ оплаты должен быть выбран;
email и phone должны быть непустыми на втором шаге оформления;
address должен быть непустым на первом шаге оформления.

###### Слой коммуникации

В приложении реализован отдельный коммуникационный слой, отвечающий за взаимодействие с сервером API «Web-Larek».

Для работы с сервером создан класс `LarekApi`.

Класс:

выполняет запрос на получение списка товаров (GET /product/);
отправляет данные заказа на сервер (POST /order/);

Класс использует композицию: в конструктор передаётся объект, реализующий интерфейс IApi.
Внутри используются методы get и post этого объекта.
Он отвечает только за коммуникацию с сервером.

###### Слой представления (View)

# Базовый класс Card

Базовый класс для отображения карточки товара.

Наследуются:
`CardCatalog`
`CardPreview`
`CardBasket`

Установка изображения, названия, цены

Поля
`container: HTMLElement — корневой DOM-элемент`
`titleElement: HTMLElement`
`priceElement: HTMLElement`
`imageElement: HTMLImageElement`

Методы
`set title(value: string)`
`set price(value: number | null)`
`set image(value: string)`
`render(data: Partial<IProduct>): HTMLElement`

# CardCatalog

Отображение карточки товара в каталоге.
Дополнительно к базовому классу - обработка клика по карточке

`card:select` — при клике на карточку

# CardPreview

Отображение детальной карточки товара в модальном окне.

Дополнительный функционал - кнопка «Купить», 
кнопка «Удалить из корзины»,
блокировка кнопки при отсутствии цены

`product:add`
`product:remove`

# CardBasket

Отображение товара внутри корзины.

Дополнительный функционал - кнопка удаления товара из корзины

`basket:remove`

# Gallery

Контейнер для отображения каталога товаров.

Поля
`catalogElement: HTMLElement`

Методы
`set catalog(items: HTMLElement[])`

Компонент принимает уже подготовленные карточки.

# Header

Шапка сайта.

Отображение счётчика товаров в корзине, обработка клика по кнопке корзины

Поля
`basketButton: HTMLButtonElement`
`counterElement: HTMLElement`

Методы
`set counter(value: number)`


`basket:open` - событие

# Modal

Отображение модального окна.

Открытие модального окна, закрытие модального окна, закрытие по клику вне области, закрытие по кнопке «крестик»

Методы
`open(content: HTMLElement)`
`close()`

`modal:close` - событие

# BasketView

Отображение корзины в модальном окне.

Отображение списка товаров, отображение суммы, блокировка кнопки оформления при пустой корзине, отображение текста «Корзина пуста»

Методы
`set items(items: HTMLElement[])`
`set total(value: number)`

`order:start` - событие

# Базовый класс Form

Общий функционал для форм.

Наследуются:
`OrderForm`
`ContactsForm`

Обработка submit, отображение ошибок, управление состоянием кнопки

Методы
`set errors(errors: Record<string, string>)`
`set valid(isValid: boolean)`

# OrderForm

Первый шаг оформления заказа.

Поля
поле выбора способа оплаты, поле ввода адреса

`order:submit`
`order:change`

# ContactsForm

Второй шаг оформления заказа.

Поля
email, телефон

`contacts:submit`
`contacts:change`

Каждое действие пользователя должно генерировать событие:

клик -> ввод данных -> отправка формы -> закрытие модального окна

Все события обрабатываются в слое Presenter.

# Компонент подтверждения заказа


Форма подтверждения заказа реализована через шаблон #success.
Отображается после успешной отправки данных на сервер.

Содержит:
итоговую сумму заказа
кнопку закрытия модального окна

После нажатия кнопки:
модальное окно закрывается
пользователь возвращается к каталогу